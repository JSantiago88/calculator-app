import { getResult } from '../../helpers/getResult';

describe('Test in getResult', () => {

    // TODO check bug with JEST and replaceAll function 

   /*  test('should return the result correctly for a sum', () => {

        let initialValue = [8, "+", 9];
        const result = getResult(initialValue);

        expect(result).toEqual(["17,00"]);
    });

    test('should return the result correctly for a subtraction', () => {

        let initialValue = [1, 5, "-", 5];
        const result = getResult(initialValue);

        expect(result).toEqual(["10,00"]);
    });

    test('should return the result correctly for a multiplication', () => {

        let initialValue = [5, "x", 6];
        const result = getResult(initialValue);

        expect(result).toEqual(["30,00"]);
    });

    test('should return the result correctly for a division', () => {

        let initialValue = [9, 6, "/", 3];
        const result = getResult(initialValue);

        expect(result).toEqual(["32,00"]);
    });

    test('should return the result correctly for a mixed operation', () => {

        let initialValue = [5, "+", 7, "+", 3, "x", 2, "+", 4, "+", 5, "-", 1, "/", 2];
        const result = getResult(initialValue);

        expect(result).toEqual(["26,50"]);
    }); */
});
